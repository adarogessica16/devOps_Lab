apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: devops-lab
data:
  # Configuración no sensible del backend
  PORT: "3000"
  NODE_ENV: "production"
  DB_HOST: "db-service"
  DB_PORT: "5432"
