apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: devops-lab
  labels:
    app: postgres
spec:
  # ClusterIP: solo accesible dentro del clúster
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432